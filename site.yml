---
- name: Install neofetch as sudo
  hosts: all
  become: true
  become_user: root
  tasks:
    - name: Install neofetch
      ansible.builtin.package:
        name: neofetch
        state: present
